<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta
            name="viewport"
            content="width=device-width, initial-scale=1.0"
        />
        <link
            rel="stylesheet"
            href="/css/user.css"
        />
        <title>Egy Felhasználó Módositása</title>
    </head>
    <body>
        <%- include('nav.ejs') %>
        <div id="user-kontener">
            <h1>Egy Felhasználó Módositása</h1>
            <form>
                <table>
                    <tr>
                        
                    </tr>
                    <tr>
                        <td>Név:</td>
                        <td>
                            <input
                                type="text"
                                id="nev"
                                value="<%= user.nev %>"
                            />
                        </td>
                    </tr>
                    <tr>
                        <td>Email:</td>
                        <td>
                            <input
                                type="text"
                                id="email"
                                value="<%= user.email %>"
                            />
                        </td>
                    </tr>
                    <!-- <tr>
                        <td>Jelszo:</td>
                        <td>
                            <input
                                type="text"
                                id="jelszo"
                                value="<%= user.jelszo %>"
                            />
                        </td>
                    </tr> -->
                    
                    
                    <!-- <tr>
                        <td>Kép:</td>
                        <td>
                            <input
                                type="text"
                                id="kep"
                                value="<%= user.kep %>"
                            />
                        </td>
                    </tr> -->

                    <tr>
                            <td>Admin:</td>
                            <td>
                                <label for="admin_igen">Igen</label>
                                <input type="radio" id="admin_igen"
                                name="admin_<%= user.email%>" value="true" <% if
                                (user.admin === true) { %> checked <% } %> />

                                <label for="admin_nem">Nem</label>
                                <input type="radio" id="admin_nem"
                                name="admin_<%= user.email%>" value="false" <%
                                if (user.admin === false) { %> checked <%} %> />
                            </td>
                        </tr>
                    
                    <tr>
                        <td>
                            <button
                                onclick="modosit(event, '<%= user._id %>', '<%= user.nev %>', '<%= user.email %>', 'admin_<%= user.email%>')"
                            >
                                Módosít
                            </button>
                        </td>
                        <td></td>
                    </tr>
                </table>
            </form>
        </div>
        <script src="/js/user-modosit.js"></script>
    </body>
</html>
